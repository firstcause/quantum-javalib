<?xml version="1.0"?>

<MutinyApp
        name="mutiny_test"
        entrypoint="showMenu('/mode_menu');" >

    <MenuDef name="mode_menu">
        <MenuItem onClick="showMenu('/trainer_mode')">
            menu_trainer_mode
        </MenuItem>
        <MenuItem onClick="showMenu('/patrol_mode')">
            menu_patrol_mode
        </MenuItem>
    </MenuDef>

    <MenuDef name="patrol_mode">
        <MenuItem onClick="doSomething(someparameter)">
            menu_administrative
        </MenuItem>
        <MenuItem onClick="doSomething(someparameter)">
            menu_new_deployment
        </MenuItem>
    </MenuDef>

</MutinyApp>
